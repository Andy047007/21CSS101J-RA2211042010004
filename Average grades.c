#include <stdio.h>
 int main() {

 int grade;
 int total;
 int average;

 total = 0;

while (counter<=10) {
    printf( "%s", "Enter grade: " );
    scanf( "%d", &grade );
    total = total + grade;
    counter=counter+1;
    }
}

